export class Sound {
  title: string;
  file: string;
  type: string;

  constructor(title: string, file: string, type: string) {
    this.title = title;
    this.file = file;
    this.type = type;
  }
}
